# On macOS 26 (Tahoe), the system linker can't find libiconv on its own.
# Point at Homebrew's libiconv via its unversioned symlink so this survives
# brew upgrades.
[target.aarch64-apple-darwin]
rustflags = ["-L", "/opt/homebrew/opt/libiconv/lib"]
